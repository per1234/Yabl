dependencies:
  cache_directories:
    - extras/verify_examples/Arduino
  pre:
    - ./install_arduino.sh
test:
  pre:
    - cd extras/unit_test; make
  override:
    - ./extras/unit_test/YablTest
    - ./verify_examples.sh
checkout:
  post:
    - git submodule init
    - git submodule update
